--- a/src/app/[locale]/page.tsx
+++ b/src/app/[locale]/page.tsx
@@ -1,44 +1,9 @@
 import { notFound } from 'next/navigation'
-
-import Header from '@/components/layout/Header'
-import Footer from '@/components/layout/Footer'
-import About from '@/components/sections/About'
-import CapabilityMarquee from '@/components/sections/CapabilityMarquee'
-import Contact from '@/components/sections/Contact'
-import Hero from '@/components/sections/Hero'
-import Process from '@/components/sections/Process'
-import Services from '@/components/sections/Services'
-import Technology from '@/components/sections/Technology'
-import WhyWorkWithMe from '@/components/sections/WhyWorkWithMe'
-import Work from '@/components/sections/Work'
+import StudioHome from '@/components/studio/StudioHome'
 import { getDictionary, isLocale } from '@/lib/i18n'
 
-interface LocalePageProps {
-  params: Promise<{ locale: string }>
+export default async function LocalePage({ params }: { params: Promise<{ locale: string }> }) {
+  const { locale } = await params
+  if (!isLocale(locale)) notFound()
+  return <StudioHome locale={locale} dictionary={getDictionary(locale)} />
 }
-
-export default async function LocalePage({ params }: LocalePageProps) {
-  const { locale } = await params
-
-  if (!isLocale(locale)) notFound()
-
-  const dictionary = getDictionary(locale)
-
-  return (
-    <>
-      <Header locale={locale} dictionary={dictionary} />
-      <main className="min-h-screen overflow-x-clip bg-canvas text-text">
-        <Hero locale={locale} dictionary={dictionary} />
-        <CapabilityMarquee dictionary={dictionary} />
-        <Services locale={locale} dictionary={dictionary} />
-        <Work locale={locale} dictionary={dictionary} />
-        <Process dictionary={dictionary} />
-        <WhyWorkWithMe dictionary={dictionary} />
-        <Technology dictionary={dictionary} />
-        <About dictionary={dictionary} />
-        <Contact locale={locale} dictionary={dictionary} />
-      </main>
-      <Footer locale={locale} dictionary={dictionary} />
-    </>
-  )
-}
--- /dev/null
+++ b/src/components/studio/StudioHome.tsx
@@ -0,0 +1,72 @@
+import Link from 'next/link'
+import MobileMenu, { type HeaderNavigationItem } from '@/components/layout/MobileMenu'
+import Footer from '@/components/layout/Footer'
+import ContactForm from '@/components/contact/ContactForm'
+import LanguageSwitcher from '@/components/ui/LanguageSwitcher'
+import ReferenceScreen from './ReferenceScreen'
+import { navigationOrder, serviceOrder, processOrder, whyOrder, type SiteDictionary } from '@/content'
+import { siteConfig } from '@/config/site'
+import { getDirection, type Locale } from '@/lib/i18n'
+import './studio.css'
+
+function Arrow() { return <span className="studio-arrow" aria-hidden="true">↗</span> }
+function Heading({ kicker, title, description }: { kicker: string; title: string; description?: string }) {
+  return <div className="studio-section-heading"><p className="studio-eyebrow">{kicker}</p><h2>{title}</h2>{description && <p>{description}</p>}</div>
+}
+function BrowserBar({ title }: { title: string }) {
+  return <div className="studio-browser-bar"><span className="studio-window-dots">•••</span><span dir="ltr">{title}</span><span aria-hidden="true">↗</span></div>
+}
+export default function StudioHome({ locale, dictionary: d }: { locale: Locale; dictionary: SiteDictionary }) {
+  const he = locale === 'he'
+  const t = (hebrew: string, english: string) => he ? hebrew : english
+  const direction = getDirection(locale)
+  const navigationItems: HeaderNavigationItem[] = navigationOrder.map(key => ({ key, label: d.navigation[key], href: key === 'guides' ? `/${locale}/guides` : `/${locale}#${key}` }))
+  const serviceLinks: Record<string, string> = { websites: `/${locale}/services/websites`, automation: `/${locale}/services/automation`, ai: `/${locale}/services/automation` }
+  const serviceSymbols = ['↗', '⊞', '◫', '⇄', '✳']
+  const projects = [
+    { name: 'Finance', tone: 'finance', source: 'finance', category: t('ניהול פיננסי', 'Personal finance'), title: t('התמונה הפיננסית.\nסוף סוף ברורה.', 'Your finances.\nFinally in focus.'), description: t('הכנסות, הוצאות ותקציבים במרחב אחד. ממשק שהופך נתונים מורכבים לתמונה שקל להבין ולפעול לפיה.', 'Income, expenses and budgets in one workspace. Turning complex data into a clear picture you can act on.'), tags: ['Dashboard', 'RTL', 'Design system'] },
+    { name: 'Panda', tone: 'panda', source: 'panda', category: t('מערכת עסקית', 'Business platform'), title: t('פחות התעסקות.\nיותר סדר.', 'Less busywork.\nMore clarity.'), description: t('סביבת עבודה לניהול מסמכים וחשבוניות, עם סדרי עדיפויות ברורים ותמונה אחת של מה מוכן, מה חסר ומה דורש תשומת לב.', 'A workspace for documents and invoices, with clear priorities and one view of what is ready, missing or needs attention.'), tags: ['Workspace', 'Documents', 'UX / UI'] },
+    { name: 'TradingLog', tone: 'trading', source: he ? 'trading-he' : 'trading-en', category: t('יומן מסחר', 'Trading journal'), title: t('כל עסקה היא\nהזדמנות ללמוד.', 'Every trade.\nSomething to learn.'), description: t('מסחר, השקעות וניתוח ביצועים בסביבת עבודה אחת. היררכיה מדויקת שמכניסה בהירות לעולם עשיר בנתונים.', 'Trading, investing and performance analysis in one workspace. A considered hierarchy that brings clarity to data.'), tags: ['Analytics', 'Data visualization', 'HE / EN'] },
+    { name: 'LifeOS', tone: 'life', source: 'lifeos', category: t('אפליקציה אישית', 'Personal app'), title: t('קצת יותר מקום\nלדברים החשובים.', 'Make room for\nwhat matters.'), description: t('משימות, הרגלים ותכנון יומי בחוויה רגועה, שעוזרת להתקדם בלי להעמיס.', 'Tasks, habits and daily planning in a calm experience that helps you move forward without the noise.'), tags: ['Mobile', 'Daily planning'] },
+    { name: 'LimitPact', tone: 'limitpact', source: 'limitpact-landing', category: t('דסקטופ ואתר מוצר', 'Desktop & product website'), title: t('גבולות ברורים.\nמסחר עם משמעת.', 'Clear limits.\nDisciplined trading.'), description: t('כלי לניטור מגבלות מסחר, עם תמונת מצב נגישה ושפה עיצובית שממשיכה מהאפליקציה לאתר המוצר.', 'A tool for monitoring trading limits, with an accessible status view and a design language that connects desktop and web.'), tags: ['Desktop app', 'Product website'] },
+  ]
+  return <div className="studio-home">
+    <a href="#main-content" className="studio-skip">{t('דלגו לתוכן', 'Skip to content')}</a>
+    <header className="studio-header"><div className="studio-container studio-header-inner">
+      <Link className="studio-brand" href={`/${locale}`} aria-label={d.identity.name}><span className="studio-brand-mark" dir="ltr">oz<span>.</span></span><span className="studio-brand-name">{d.identity.name}<small dir="ltr">DESIGN & DEVELOPMENT</small></span></Link>
+      <nav className="studio-desktop-nav" aria-label={d.header.menuLabel}>{navigationItems.filter(item => item.key !== 'contact').map(item => <Link key={item.key} href={item.href}>{item.label}</Link>)}</nav>
+      <div className="studio-header-actions"><div className="studio-desktop-language"><LanguageSwitcher locale={locale} labels={d.header} /></div><a className="studio-header-cta" href={`/${locale}#contact`}>{d.header.contactCta}<Arrow /></a><MobileMenu locale={locale} identityName={d.identity.name} labels={d.header} primaryCta={d.hero.primaryCta} contactHref={`/${locale}#contact`} navigationItems={navigationItems} /></div>
+    </div></header>
+    <main id="main-content">
+      <section className="studio-hero" id="top"><div className="studio-container">
+        <div className="studio-hero-copy"><p className="studio-availability"><span aria-hidden="true" />{d.hero.availability}</p>
+          <h1>{t('רעיונות גדולים.', 'Big ideas.')}<br /><span>{t('מוצרים מדויקים.', 'Considered products.')}</span></h1>
+          <p className="studio-hero-description">{t('אני עוזר לעסקים להפוך רעיונות לאתרים, מערכות ואפליקציות — עם חשיבה מוצרית, עיצוב מוקפד ופיתוח עד הפרט האחרון.', 'I help businesses turn ideas into websites, systems and apps — with product thinking, thoughtful design and care in every detail.')}</p>
+          <div className="studio-hero-actions"><a className="studio-button" href={`/${locale}#contact`}>{t('בואו נדבר על הרעיון שלכם', 'Let’s talk about your idea')}<Arrow /></a><a className="studio-text-link" href={`/${locale}#work`}>{t('לפרויקטים שלי', 'Explore my work')}<span aria-hidden="true">↓</span></a></div>
+        </div>
+        <div className="studio-hero-stage" aria-hidden="true"><div className="studio-hero-orbit" />
+          <div className="studio-hero-desktop"><BrowserBar title="Finance · Personal workspace" /><ReferenceScreen name="finance" title="Finance dashboard" eager /></div>
+          <div className="studio-hero-phone"><ReferenceScreen name="lifeos" title="LifeOS daily overview" width={402} height={856} eager /></div>
+          <div className="studio-hero-side-note"><span className="studio-note-star">✳</span><span>{t('מהרעיון הראשון.\nעד המסך האחרון.', 'From the first idea.\nTo the final detail.')}</span></div>
+        </div>
+        <div className="studio-capabilities"><span>{t('מחבר בין עיצוב לטכנולוגיה', 'Where design meets technology')}</span><ul>{[d.capabilityMarquee.websites, d.capabilityMarquee.businessSystems, d.capabilityMarquee.applications, d.capabilityMarquee.automation, d.capabilityMarquee.ai].map(item => <li key={item}>{item}</li>)}</ul></div>
+      </div></section>
+      <section className="studio-section studio-work" id="work"><div className="studio-container">
+        <div className="studio-heading-row"><Heading kicker={t('עבודות נבחרות', 'Selected work')} title={t('כל מוצר, עולם שלם.', 'Every product, its own world.')} description={t('ממשקים שנבנו סביב האנשים שמשתמשים בהם. מפיננסים ונתונים, ועד ניהול היום־יום.', 'Interfaces built around the people who use them. From finances and data to the flow of everyday life.')} /><span className="studio-section-meta" dir="ltr">DESIGNED WITH INTENT.<br />BUILT WITH CARE.</span></div>
+        <div className="studio-project-grid">{projects.map(project => <article key={project.name} className={`studio-project studio-project--${project.tone}`}>
+          <div className="studio-project-copy"><div className="studio-project-name"><bdi dir="ltr">{project.name}</bdi><span>{project.category}</span></div><h3>{project.title}</h3><p>{project.description}</p><ul className="studio-tags">{project.tags.map(tag => <li key={tag}>{tag}</li>)}</ul><a className="studio-project-link" href={`/project-previews/${project.source}.html`} target="_blank" rel="noopener noreferrer">{t('לצפייה במסך המלא', 'View the full design')}<Arrow /></a></div>
+          <div className="studio-project-art">
+            {['finance', 'panda', 'trading'].includes(project.tone) && <div className="studio-browser"><BrowserBar title={`${project.name} / Overview`} /><ReferenceScreen name={project.source} title={`${project.name} — ${t('עיצוב הממשק', 'Interface design')}`} height={project.tone === 'panda' ? 900 : 960} /></div>}
+            {project.tone === 'life' && <><div className="studio-life-word" aria-hidden="true" dir="ltr">A little<br /><em>more life.</em></div><div className="studio-life-phone"><ReferenceScreen name="lifeos" title={t('מסך היום שלי ב־LifeOS', 'LifeOS Today screen')} width={402} height={856} /></div></>}
+            {project.tone === 'limitpact' && <><div className="studio-limit-web"><ReferenceScreen name="limitpact-landing" title="LimitPact product website design" /></div><a className="studio-limit-app" href="/project-previews/limitpact.html" target="_blank" rel="noopener noreferrer" aria-label={t('הגדלת עיצוב אפליקציית LimitPact', 'View the LimitPact desktop design')}><ReferenceScreen name="limitpact" title="LimitPact desktop design" width={380} height={620} /></a><span className="studio-art-caption" dir="ltr">ONE PRODUCT. ONE DESIGN LANGUAGE.</span></>}
+          </div>
+        </article>)}</div>
+      </div></section>
+      <section className="studio-section studio-services" id="services"><div className="studio-container studio-services-inner"><Heading kicker={d.services.kicker} title={t('מה צריך לקרות\nכדי שתתקדמו?', 'What will move\nyou forward?')} description={d.services.description} /><div className="studio-service-list">{serviceOrder.map((key, index) => { const item = d.services.items[key];const content = <><span className="studio-service-symbol" aria-hidden="true">{serviceSymbols[index]}</span><div className="studio-service-copy"><h3>{item.title}</h3><p>{item.description}</p></div>{serviceLinks[key] && <Arrow />}</>;return serviceLinks[key] ? <Link href={serviceLinks[key]} className="studio-service" key={key}>{content}</Link> : <div className="studio-service" key={key}>{content}</div> })}</div></div></section>
+      <section className="studio-section studio-process" id="process"><div className="studio-container"><div className="studio-heading-row"><Heading kicker={d.process.kicker} title={d.process.title} /><p className="studio-process-intro">{d.process.description}</p></div><ol className="studio-process-grid" dir={direction}>{processOrder.map(key => { const step = d.process.items[key];return <li key={key}><span className="studio-step-number"><bdi dir="ltr">{step.number}</bdi></span><h3>{step.title}</h3><p>{step.description}</p></li> })}</ol></div></section>
+      <section className="studio-section studio-about" id="about"><div className="studio-container studio-about-grid"><div className="studio-about-card"><span className="studio-about-monogram" dir="ltr" aria-hidden="true">oz<span>.</span></span><div><span className="studio-eyebrow">{t('נעים להכיר', 'Good to meet you')}</span><h2>{d.identity.name}</h2><p dir="ltr">DESIGN THINKING.<br />DEVELOPER MINDSET.</p></div><span className="studio-about-star" aria-hidden="true">✳</span></div><div className="studio-about-body"><p className="studio-eyebrow">{d.about.kicker}</p><h3>{t('אותו אדם שחושב איתכם.\nגם בונה איתכם.', 'The person who thinks with you.\nBuilds with you.')}</h3><p>{d.about.body}</p><ul>{whyOrder.map(key => <li key={key}><span aria-hidden="true">↗</span>{d.whyWorkWithMe.items[key]}</li>)}</ul></div></div></section>
+      <section className="studio-technology" aria-label={d.technology.title}><div className="studio-container"><p>{d.technology.title}</p><ul dir="ltr">{d.technology.items.map(item => <li key={item}>{item}</li>)}</ul></div></section>
+      <section className="studio-section studio-contact" id="contact"><div className="studio-container studio-contact-grid"><div className="studio-contact-copy"><p className="studio-eyebrow">{d.navigation.contact}</p><h2>{t('הדבר הגדול הבא\nמתחיל בשיחה.', 'The next big thing\nstarts with a conversation.')}</h2><p>{d.contact.description}</p><a className="studio-contact-email" href={`mailto:${siteConfig.email}`} dir="ltr">{siteConfig.email}<Arrow /></a><span className="studio-contact-spark" aria-hidden="true">✳</span></div><div className="studio-contact-form"><ContactForm locale={locale} content={d.contact.form} /></div></div></section>
+    </main><Footer locale={locale} dictionary={d} />
+  </div>
+}
--- /dev/null
+++ b/src/components/studio/ReferenceScreen.tsx
@@ -0,0 +1,31 @@
+'use client'
+
+import { useEffect, useRef, type CSSProperties } from 'react'
+
+interface ReferenceScreenProps {
+  name: string
+  title: string
+  width?: number
+  height?: number
+  eager?: boolean
+}
+
+/** Passive, isolated copy of the supplied product design at its original size. */
+export default function ReferenceScreen({ name, title, width = 1440, height = 960, eager = false }: ReferenceScreenProps) {
+  const container = useRef<HTMLDivElement>(null)
+  useEffect(() => {
+    const element = container.current
+    if (!element) return
+    const fit = () => {
+      element.style.setProperty('--screen-scale', String(element.clientWidth / width))
+      element.dataset.ready = 'true'
+    }
+    fit()
+    const observer = new ResizeObserver(fit)
+    observer.observe(element)
+    return () => observer.disconnect()
+  }, [width])
+  return <div ref={container} className="studio-screen" data-screen-width={width} style={{ aspectRatio: `${width} / ${height}` } as CSSProperties}>
+    <iframe src={`/project-previews/${name}.html`} title={title} width={width} height={height} loading={eager ? 'eager' : 'lazy'} sandbox="" tabIndex={-1} />
+  </div>
+}
--- /dev/null
+++ b/src/components/studio/studio.css
@@ -0,0 +1,288 @@
+html:has(.studio-home), body:has(.studio-home) { background: #f8f9fc; }
+.studio-home {
+  --studio-bg: #f8f9fc;
+  --studio-surface: #eef1f7;
+  --studio-text: #202c40;
+  --studio-muted: #647086;
+  --studio-blue: #345fe9;
+  --studio-line: #202c401b;
+  background: var(--studio-bg);
+  color: var(--studio-text);
+  font-size: 1rem;
+  line-height: 1.75;
+}
+.studio-home a { text-decoration: none; }
+.studio-home :is(a,button,input,textarea):focus-visible { outline: 2px solid var(--studio-blue); outline-offset: 5px; }
+.studio-home ::selection { background: #345fe9; color: white; }
+.studio-container { width: min(100% - 112px, 1328px); margin-inline: auto; }
+.studio-home section[id], .studio-home main[id] { scroll-margin-top: 96px; }
+.studio-skip { position: fixed; z-index: 100; top: -100px; inset-inline-start: 24px; background: #fff; color: var(--studio-text); padding: 12px 24px; border-radius: 8px; }
+.studio-skip:focus { top: 16px; }
+.studio-header { position: sticky; top: 0; z-index: 60; background: #f8f9fcf2; border-bottom: 1px solid var(--studio-line); }
+.studio-header-inner { min-height: 88px; display: flex; align-items: center; justify-content: space-between; gap: 24px; }
+.studio-brand { display: flex; align-items: center; gap: 15px; flex-shrink: 0; }
+.studio-brand-mark { font: 850 3rem/1 Arial,sans-serif; letter-spacing: -.09em; }
+.studio-brand-mark span { color: var(--studio-blue); }
+.studio-brand-name { font-size: .95rem; font-weight: 600; line-height: 1.5; }
+.studio-brand-name small { display: block; font: .6rem/1.5 var(--font-ibm-plex-mono),monospace; letter-spacing: .035em; color: var(--studio-muted); margin-top: 3px; }
+.studio-desktop-nav { display: flex; gap: 27px; align-items: center; }
+.studio-desktop-nav a { color: #536078; font-size: .9rem; transition: color .18s; white-space: nowrap; }
+.studio-desktop-nav a:hover { color: var(--studio-blue); }
+.studio-header-actions { display: flex; align-items: center; gap: 22px; }
+.studio-header-cta { display: inline-flex; align-items: center; justify-content: center; gap: 22px; color: var(--studio-text); border: 1px solid #202c4030; border-radius: 100px; padding: 9px 19px; font-size: .9rem; white-space: nowrap; }
+.studio-header-cta:hover { background: #edf1fb; border-color: #345fe960; }
+.studio-arrow { font: 400 1.4rem/1 Arial,sans-serif; display: inline-block; }
+[dir=rtl] .studio-arrow { transform: scaleX(-1); }
+.studio-hero { overflow: clip; }
+.studio-hero-copy { position: relative; z-index: 1; text-align: center; padding-top: 74px; }
+.studio-availability { display: inline-flex; gap: 9px; align-items: center; font-size: .78rem; color: #596a7e; margin-bottom: 26px; padding: 6px 14px; border: 1px solid var(--studio-line); border-radius: 50px; }
+.studio-availability > span { width: 6px; height: 6px; border-radius: 50%; background: #4c817b; }
+.studio-hero h1 { font-size: clamp(3.3rem,7.1vw,6.4rem); line-height: 1.07; font-weight: 720; letter-spacing: -.055em; margin: 0; text-wrap: balance; }
+.studio-hero h1 > span { color: var(--studio-blue); }
+.studio-hero-description { margin: 28px auto 0; color: var(--studio-muted); font-size: 1.08rem; line-height: 1.85; max-width: 590px; text-wrap: pretty; }
+.studio-hero-actions { display: flex; gap: 30px; align-items: center; justify-content: center; margin-top: 28px; flex-wrap: wrap; }
+.studio-button { display: inline-flex; justify-content: space-between; align-items: center; gap: 30px; background: var(--studio-blue); color: #fff; border-radius: 100px; padding: 14px 23px; font-size: .93rem; font-weight: 500; box-shadow: 0 6px 16px #345fe918; transition: background .2s,transform .2s; }
+.studio-button:hover { background: #244dd1; transform: translateY(-2px); }
+.studio-text-link { display: inline-flex; gap: 15px; align-items: center; color: #465771; padding-block: 8px; font-size: .93rem; }
+.studio-text-link:hover { color: var(--studio-blue); }
+.studio-hero-stage { height: clamp(370px,46vw,570px); position: relative; margin-top: 54px; direction: ltr; }
+.studio-hero-orbit { position: absolute; inset: 5% -18% -18%; border-radius: 50%; background: radial-gradient(ellipse at 50% 80%,#dbe4ff75,transparent 67%); border: 1px solid #cbd5e530; }
+.studio-hero-desktop { position: absolute; width: 79%; left: 8%; top: 0; border: 5px solid #fff; border-radius: 17px; box-shadow: 0 32px 85px #293e6a20,0 2px 4px #293e6a12; overflow: hidden; transform: perspective(1600px) rotateX(3deg); }
+.studio-hero-phone { position: absolute; width: 19.5%; max-width: 248px; right: 1%; top: 17%; transform: rotate(5deg); border-radius: 38px; box-shadow: 0 22px 45px #293e6a28; overflow: hidden; }
+.studio-hero-side-note { position: absolute; left: -1%; bottom: 10%; display: flex; align-items: center; gap: 15px; color: #68748a; font-size: .75rem; line-height: 1.7; white-space: pre-line; transform: rotate(-7deg); background: #f8f9fce8; padding: 14px 18px; border: 1px solid #e1e6f0; border-radius: 14px; }
+.studio-note-star { color: #6e86ca; font: 3rem/1 Georgia,serif; }
+.studio-capabilities { position: relative; z-index: 2; display: flex; justify-content: space-between; gap: 30px; align-items: center; border-top: 1px solid var(--studio-line); padding-block: 25px; background: var(--studio-bg); }
+.studio-capabilities > span { color: #6c788b; font-size: .8rem; }
+.studio-capabilities ul { display: flex; flex-wrap: wrap; gap: 26px; font-size: .9rem; color: #495a72; }
+.studio-capabilities li + li::before { content: '·'; margin-inline-end: 26px; color: #96a4bc; }
+.studio-section { padding-block: 108px; }
+.studio-eyebrow { font-size: .82rem; font-weight: 600; color: var(--studio-blue); margin-bottom: 20px; }
+.studio-section-heading h2 { font-size: clamp(2.2rem,4.2vw,3.6rem); line-height: 1.2; font-weight: 650; letter-spacing: -.04em; white-space: pre-line; text-wrap: balance; }
+.studio-section-heading > p:not(.studio-eyebrow) { font-size: 1rem; color: var(--studio-muted); max-width: 530px; margin-top: 24px; line-height: 1.85; }
+.studio-heading-row { display: flex; justify-content: space-between; align-items: flex-end; gap: 55px; margin-bottom: 54px; }
+.studio-section-meta { font: .65rem/1.85 var(--font-ibm-plex-mono),monospace; letter-spacing: .04em; color: #7f8aa0; flex-shrink: 0; padding-bottom: 8px; }
+.studio-work { border-top: 1px solid var(--studio-line); }
+.studio-project-grid { display: grid; grid-template-columns: repeat(2,minmax(0,1fr)); gap: 28px; }
+.studio-project { background: white; min-width: 0; border: 1px solid var(--studio-line); border-radius: 20px; overflow: hidden; display: flex; flex-direction: column; }
+.studio-project-copy { padding: 40px; display: flex; flex-direction: column; align-items: flex-start; }
+.studio-project-name { display: flex; align-items: center; gap: 13px; margin-bottom: 30px; flex-wrap: wrap; }
+.studio-project-name bdi { font: 650 1.05rem/1.4 Arial,sans-serif; letter-spacing: -.025em; }
+.studio-project-name > span { font-size: .73rem; color: #65748b; border-inline-start: 1px solid #dfe4ed; padding-inline-start: 13px; }
+.studio-project h3 { font-size: clamp(1.9rem,2.65vw,2.65rem); line-height: 1.2; letter-spacing: -.035em; font-weight: 620; white-space: pre-line; margin-bottom: 22px; }
+.studio-project-copy > p { font-size: .94rem; line-height: 1.85; color: var(--studio-muted); max-width: 420px; }
+.studio-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-block: 22px 28px; }
+.studio-tags li { font: .61rem/1.5 var(--font-ibm-plex-mono),monospace; padding: 6px 9px; border-radius: 50px; background: #f4f6fa; color: #65748b; direction: ltr; }
+.studio-project-link { display: inline-flex; align-items: center; gap: 14px; font-size: .86rem; font-weight: 500; margin-top: auto; color: #345fe9; }
+.studio-project-link:hover { text-decoration: underline; text-underline-offset: 5px; }
+.studio-project-art { min-width: 0; position: relative; background: var(--studio-surface); padding: 30px; display: flex; align-items: center; justify-content: center; overflow: hidden; }
+.studio-project--finance,.studio-project--panda,.studio-project--trading { grid-column: 1/-1; display: grid; grid-template-columns: minmax(0,.75fr) minmax(0,1.65fr); }
+.studio-project--panda .studio-project-copy { order: 2; }
+.studio-project--panda { grid-template-columns: minmax(0,1.65fr) minmax(0,.75fr); }
+.studio-project--panda .studio-project-art { order: 1; }
+.studio-project--finance .studio-project-copy,.studio-project--panda .studio-project-copy,.studio-project--trading .studio-project-copy { padding: 42px 34px; }
+.studio-browser { width: 100%; border: 1px solid #fff; border-radius: 10px; overflow: hidden; box-shadow: 0 18px 35px #27395e18,0 2px 5px #27395e0a; transition: transform .35s; }
+.studio-browser-bar { height: 29px; background: #fff; display: flex; justify-content: space-between; align-items: center; direction: ltr; gap: 10px; padding: 0 13px; color: #8290a7; font: .57rem/1.4 var(--font-ibm-plex-mono),monospace; }
+.studio-window-dots { color: #cad1df; letter-spacing: 3px; font: 16px/1 Arial,sans-serif; }
+.studio-screen { width: 100%; position: relative; overflow: hidden; direction: ltr; }
+.studio-screen iframe { display: block; position: absolute; top: 0; left: 0; max-width: none; border: 0; transform: scale(var(--screen-scale,1)); transform-origin: 0 0; pointer-events: none; opacity: 0; }
+.studio-screen[data-ready=true] iframe { opacity: 1; }
+.studio-project--life .studio-project-art,.studio-project--limitpact .studio-project-art { height: 475px; margin-top: auto; flex-shrink: 0; }
+.studio-life-word { position: absolute; left: 8%; top: 22%; color: #74829b; font: 400 clamp(2.6rem,4vw,4rem)/1.07 Georgia,serif; letter-spacing: -.06em; }
+.studio-life-word em { color: #748b96; }
+.studio-life-phone { position: absolute; width: 225px; right: 10%; top: 30px; transform: rotate(-5deg); border-radius: 32px; overflow: hidden; box-shadow: 0 20px 40px #293e6a25; }
+.studio-limit-web { position: absolute; top: 40px; width: 86%; left: 7%; border: 4px solid white; border-radius: 10px; box-shadow: 0 15px 30px #293e6a1a; overflow: hidden; }
+.studio-limit-app { position: absolute; width: 37%; max-width: 235px; top: 130px; right: 5%; border-radius: 16px; box-shadow: 0 14px 28px #15254830; overflow: hidden; transition: transform .25s; }
+.studio-limit-app:hover { transform: translateY(-4px); }
+.studio-art-caption { position: absolute; bottom: 23px; left: 30px; color: #79869a; font: .57rem/1.5 var(--font-ibm-plex-mono),monospace; letter-spacing: .04em; }
+.studio-services { border-top: 1px solid var(--studio-line); }
+.studio-services-inner { display: grid; grid-template-columns: 1fr 1.3fr; gap: 100px; align-items: start; }
+.studio-services .studio-section-heading { position: sticky; top: 135px; }
+.studio-service { display: flex; gap: 25px; padding-block: 30px; border-bottom: 1px solid var(--studio-line); align-items: flex-start; }
+.studio-service:first-child { border-top: 1px solid var(--studio-line); }
+.studio-service-symbol { display: flex; flex-shrink: 0; align-items: center; justify-content: center; width: 46px; height: 46px; background: #edf1fa; border: 1px solid #dee5f4; color: #4e6aab; border-radius: 12px; font: 1.5rem/1 Arial,sans-serif; }
+.studio-service-copy { flex: 1; }
+.studio-service h3 { font-size: 1.4rem; font-weight: 550; line-height: 1.35; margin-block: 6px 12px; }
+.studio-service p { font-size: .94rem; color: var(--studio-muted); line-height: 1.85; max-width: 450px; }
+.studio-service > .studio-arrow { color: #7e90b1; margin-top: 9px; }
+.studio-service:is(a):hover h3 { color: var(--studio-blue); }
+.studio-process { border-block: 1px solid var(--studio-line); }
+.studio-process-intro { color: var(--studio-muted); font-size: 1rem; max-width: 380px; }
+.studio-process-grid { display: grid; grid-template-columns: repeat(4,minmax(0,1fr)); gap: 34px; margin-top: 58px; }
+.studio-process-grid[dir=rtl] { direction: rtl; }
+.studio-process-grid[dir=ltr] { direction: ltr; }
+.studio-process-grid li { min-width: 0; position: relative; text-align: start; }
+.studio-process-grid li::before { content: ''; position: absolute; top: 27px; inset-inline-start: 72px; inset-inline-end: -20px; border-top: 1px solid #ccd5e6; }
+.studio-process-grid li:last-child::before { display: none; }
+.studio-step-number { display: flex; align-items: center; justify-content: center; width: 56px; height: 56px; border: 1px solid #d0dbf4; border-radius: 50%; background: #edf2ff; color: #4b6ec0; font: .93rem/1 var(--font-ibm-plex-mono),monospace; margin-bottom: 27px; }
+.studio-step-number bdi { unicode-bidi: isolate; }
+.studio-process-grid h3 { font-size: 1.3rem; font-weight: 550; margin-bottom: 13px; }
+.studio-process-grid p { color: var(--studio-muted); font-size: .94rem; line-height: 1.85; }
+.studio-about-grid { display: grid; grid-template-columns: .9fr 1.1fr; gap: 100px; align-items: center; }
+.studio-about-card { background: #edf1f8; border: 1px solid #dce3ef; border-radius: 20px; min-height: 480px; padding: 44px; display: flex; flex-direction: column; justify-content: space-between; position: relative; overflow: hidden; }
+.studio-about-monogram { font: 800 8.5rem/1 Arial,sans-serif; letter-spacing: -.1em; align-self: flex-end; color: #c3cee3; }
+.studio-about-monogram span { color: #7690cf; }
+.studio-about-card h2 { font-size: 2rem; font-weight: 550; margin: 12px 0 22px; }
+.studio-about-card p { font: .67rem/1.85 var(--font-ibm-plex-mono),monospace; letter-spacing: .03em; color: #7585a0; text-align: start; }
+.studio-about-star { position: absolute; left: -20px; bottom: -33px; color: #ccd7ec; font: 13rem/1 Georgia,serif; transform: rotate(18deg); }
+.studio-about-body h3 { font-size: clamp(2rem,3.2vw,3rem); line-height: 1.25; font-weight: 600; letter-spacing: -.035em; white-space: pre-line; margin-bottom: 25px; }
+.studio-about-body > p:not(.studio-eyebrow) { color: var(--studio-muted); font-size: 1rem; line-height: 1.95; }
+.studio-about-body ul { display: grid; grid-template-columns: 1fr 1fr; gap: 16px 24px; margin-top: 32px; padding-top: 28px; border-top: 1px solid var(--studio-line); }
+.studio-about-body li { display: flex; align-items: flex-start; gap: 12px; font-size: .88rem; color: #4e607d; }
+.studio-about-body li > span { color: var(--studio-blue); }
+.studio-technology .studio-container { display: flex; align-items: center; justify-content: space-between; gap: 40px; border-block: 1px solid var(--studio-line); padding-block: 26px; }
+.studio-technology p { font-size: .79rem; color: #79859a; }
+.studio-technology ul { display: flex; flex-wrap: wrap; justify-content: flex-end; gap: 27px; color: #75829a; font: .83rem/1.5 var(--font-ibm-plex-mono),monospace; }
+.studio-contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 100px; }
+.studio-contact-copy { position: relative; }
+.studio-contact-copy h2 { font-size: clamp(2.5rem,4.7vw,4.2rem); line-height: 1.17; font-weight: 620; letter-spacing: -.04em; white-space: pre-line; }
+.studio-contact-copy > p:not(.studio-eyebrow) { margin-top: 25px; max-width: 430px; color: var(--studio-muted); font-size: 1rem; line-height: 1.85; }
+.studio-contact-email { display: inline-flex; align-items: center; gap: 25px; margin-top: 30px; font: 1rem/1.5 var(--font-ibm-plex-mono),monospace; color: #4667ad; border-bottom: 1px solid #c6d1e8; padding-bottom: 8px; }
+.studio-contact-spark { display: block; margin-top: 30px; color: #b6c7ec; font: 6rem/1 Georgia,serif; }
+.studio-contact-form { background: #fff; border: 1px solid var(--studio-line); border-radius: 20px; padding: 35px; }
+.studio-contact-form form { margin: 0; max-width: none; gap: 19px; }
+.studio-contact-form label { color: #53617b; font-weight: 500; }
+.studio-contact-form :is(input,textarea) { background: #f8f9fc; border: 1px solid #dce2ec; color: #26354d; border-radius: 8px; }
+.studio-contact-form :is(input,textarea)::placeholder { color: #8590a3; }
+.studio-contact-form :is(input,textarea):focus { border-color: #6381d9; }
+.studio-contact-form button[type=submit] { background: #345fe9; color: #fff; border-radius: 50px; box-shadow: none; }
+.studio-contact-form button[type=submit]:hover { background: #244dd1; }
+.studio-contact-form p[aria-live] { color: #78859b; }
+.studio-contact-form [role=alert],.studio-contact-form .text-danger { color: #b02b32; }
+.studio-contact-form [role=status] span { color: #276b53; }
+.studio-home > footer { background: var(--studio-bg); border-color: var(--studio-line); }
+.studio-home > footer [class*=border-white] { border-color: var(--studio-line); }
+.studio-home > footer :is(p,a,li) { color: #617088; }
+.studio-home > footer :is(h2,[aria-current=page]) { color: #344a70; }
+.studio-home > footer a:hover { color: var(--studio-blue); }
+.studio-home [role=group] :is(a,span) { color: #7a879c; }
+.studio-home [role=group] [aria-current] { color: var(--studio-text); }
+.studio-home [aria-controls=mobile-menu] { color: var(--studio-text); border-color: #ccd6e7; }
+.studio-home #mobile-menu { background: #f8f9fc; color: var(--studio-text); backdrop-filter: none; }
+.studio-home #mobile-menu .text-text { color: var(--studio-text); }
+.studio-home #mobile-menu :is(button,nav a) { border-color: #202c4020; }
+@media(hover:hover) { .studio-project:hover .studio-browser { transform: translateY(-3px); } }
+@media(min-width:1024px) and (max-width:1230px) {
+  .studio-brand-name { display: none; }
+  .studio-desktop-nav { gap: 20px; }
+  .studio-header-actions { gap: 14px; }
+}
+@media(max-width:1100px) {
+  .studio-container { width: calc(100% - 64px); }
+  .studio-services-inner,.studio-about-grid,.studio-contact-grid { gap: 50px; }
+  .studio-project--finance,.studio-project--trading { grid-template-columns: minmax(0,1fr) minmax(0,1.6fr); }
+  .studio-project--panda { grid-template-columns: minmax(0,1.6fr) minmax(0,1fr); }
+  .studio-project h3 { font-size: 2rem; }
+  .studio-project-art { padding: 20px; }
+  .studio-project-copy { padding: 30px; }
+  .studio-project--finance .studio-project-copy,.studio-project--panda .studio-project-copy,.studio-project--trading .studio-project-copy { padding: 30px 26px; }
+  .studio-life-phone { width: 205px; right: 7%; top: 45px; }
+  .studio-life-word { left: 5%; font-size: 2.8rem; top: 16%; }
+  .studio-capabilities { flex-direction: column; gap: 12px; }
+}
+@media(max-width:1023px) {
+  .studio-desktop-nav,.studio-desktop-language { display: none; }
+  .studio-header-inner { min-height: 78px; }
+  .studio-header-actions { gap: 12px; }
+  .studio-hero-copy { padding-top: 56px; }
+  .studio-hero-stage { height: 45vw; min-height: 320px; }
+  .studio-hero-side-note { display: none; }
+  .studio-section { padding-block: 80px; }
+}
+@media(max-width:800px) {
+  .studio-services-inner { grid-template-columns: 1fr; gap: 30px; }
+  .studio-services .studio-section-heading { position: static; }
+  .studio-heading-row { gap: 30px; margin-bottom: 36px; }
+  .studio-section-meta { display: none; }
+  .studio-project-grid { gap: 24px; }
+  .studio-project--finance,.studio-project--panda,.studio-project--trading { display: flex; }
+  .studio-project--panda .studio-project-copy { order: 0; }
+  .studio-project--panda .studio-project-art { order: 1; }
+  .studio-project-copy,.studio-project--finance .studio-project-copy,.studio-project--panda .studio-project-copy,.studio-project--trading .studio-project-copy { padding: 30px; }
+  .studio-project-art { padding: 25px; }
+  .studio-project-name { margin-bottom: 20px; }
+  .studio-project h3 { white-space: normal; }
+  .studio-project--life,.studio-project--limitpact { grid-column: 1/-1; }
+  .studio-project--life .studio-project-art,.studio-project--limitpact .studio-project-art { height: 480px; }
+  .studio-life-phone { width: 225px; top: 32px; right: 15%; }
+  .studio-life-word { left: 10%; font-size: 4rem; }
+  .studio-limit-app { width: 34%; max-width: 235px; right: 8%; }
+  .studio-process-grid { grid-template-columns: 1fr 1fr; gap: 38px; }
+  .studio-process-grid li:nth-child(2)::before { display: none; }
+  .studio-process-intro { max-width: 300px; }
+  .studio-about-grid { grid-template-columns: 1fr; gap: 40px; }
+  .studio-about-card { min-height: 340px; flex-direction: row; align-items: center; gap: 25px; padding: 40px; }
+  .studio-about-monogram { align-self: auto; font-size: 7rem; order: 2; }
+  .studio-about-star { display: none; }
+  .studio-technology .studio-container { flex-direction: column; gap: 20px; }
+  .studio-technology ul { justify-content: center; }
+  .studio-contact-grid { grid-template-columns: 1fr; gap: 35px; }
+  .studio-contact-spark { display: none; }
+}
+@media(max-width:560px) {
+  .studio-container { width: calc(100% - 36px); }
+  .studio-header-inner { min-height: 72px; gap: 10px; }
+  .studio-brand-mark { font-size: 2.6rem; }
+  .studio-brand-name { font-size: .83rem; }
+  .studio-brand-name small { display: none; }
+  .studio-brand { gap: 12px; }
+  .studio-header-cta { padding: 7px 12px; font-size: .78rem; gap: 9px; }
+  .studio-header-cta .studio-arrow { font-size: 1.1rem; }
+  .studio-hero-copy { padding-top: 43px; }
+  .studio-availability { font-size: .68rem; padding: 5px 10px; margin-bottom: 25px; }
+  .studio-hero h1 { font-size: clamp(2.7rem,10.8vw,3.8rem); line-height: 1.13; letter-spacing: -.05em; }
+  .studio-hero-description { font-size: .95rem; max-width: 350px; margin-top: 22px; }
+  .studio-hero-actions { flex-direction: column; gap: 9px; margin-top: 23px; }
+  .studio-button { font-size: .88rem; padding: 12px 22px; }
+  .studio-text-link { font-size: .83rem; }
+  .studio-hero-stage { margin-top: 32px; height: 63vw; min-height: 230px; }
+  .studio-hero-desktop { width: 97%; left: -6%; border-width: 3px; border-radius: 10px; top: 8px; }
+  .studio-hero-phone { width: 29%; right: -1%; top: 16%; border-radius: 22px; }
+  .studio-browser-bar { height: 22px; padding-inline: 8px; font-size: .45rem; }
+  .studio-capabilities { padding-block: 20px; gap: 12px; text-align: center; }
+  .studio-capabilities > span { font-size: .75rem; }
+  .studio-capabilities ul { justify-content: center; gap: 7px 14px; font-size: .74rem; }
+  .studio-capabilities li + li::before { display: none; }
+  .studio-section { padding-block: 64px; }
+  .studio-eyebrow { font-size: .77rem; margin-bottom: 15px; }
+  .studio-section-heading h2 { font-size: 2.15rem; }
+  .studio-section-heading > p:not(.studio-eyebrow) { font-size: .94rem; margin-top: 18px; }
+  .studio-project { border-radius: 15px; }
+  .studio-project-copy,.studio-project--finance .studio-project-copy,.studio-project--panda .studio-project-copy,.studio-project--trading .studio-project-copy { padding: 26px; }
+  .studio-project h3 { font-size: 2rem; }
+  .studio-project-copy > p { font-size: .9rem; }
+  .studio-tags { margin-block: 20px 24px; }
+  .studio-project-art { padding: 15px; }
+  .studio-project--life .studio-project-art { height: 410px; }
+  .studio-project--limitpact .studio-project-art { height: 370px; }
+  .studio-life-phone { width: 183px; right: 8%; top: 35px; }
+  .studio-life-word { left: 6%; font-size: 2.65rem; }
+  .studio-limit-web { width: 92%; left: 4%; top: 25px; }
+  .studio-limit-app { width: 45%; right: 7%; top: 100px; }
+  .studio-art-caption { left: 20px; bottom: 20px; font-size: .44rem; }
+  .studio-service { gap: 17px; padding-block: 25px; }
+  .studio-service-symbol { width: 38px; height: 38px; font-size: 1.3rem; border-radius: 9px; }
+  .studio-service h3 { font-size: 1.2rem; margin-top: 4px; }
+  .studio-service p { font-size: .89rem; }
+  .studio-process .studio-heading-row { display: block; }
+  .studio-process-intro { margin-top: 22px; max-width: none; font-size: .94rem; }
+  .studio-process-grid { gap: 34px 24px; margin-top: 38px; }
+  .studio-step-number { width: 47px; height: 47px; font-size: .8rem; margin-bottom: 19px; }
+  .studio-process-grid li::before { top: 23px; inset-inline-start: 62px; inset-inline-end: -10px; }
+  .studio-process-grid h3 { font-size: 1.15rem; }
+  .studio-process-grid p { font-size: .87rem; }
+  .studio-about-card { min-height: 270px; padding: 28px; }
+  .studio-about-monogram { font-size: 5rem; }
+  .studio-about-card h2 { font-size: 1.7rem; }
+  .studio-about-card p { font-size: .5rem; }
+  .studio-about-body h3 { font-size: 2rem; }
+  .studio-about-body > p:not(.studio-eyebrow) { font-size: .94rem; }
+  .studio-about-body ul { grid-template-columns: 1fr; gap: 13px; }
+  .studio-technology ul { gap: 14px 22px; font-size: .72rem; }
+  .studio-contact-copy h2 { font-size: 2.55rem; }
+  .studio-contact-copy > p:not(.studio-eyebrow) { font-size: .94rem; }
+  .studio-contact-email { font-size: .81rem; gap: 15px; overflow-wrap: anywhere; }
+  .studio-contact-form { padding: 25px; border-radius: 15px; }
+}
+@media(prefers-reduced-motion:reduce) {
+  .studio-home * { transition: none !important; }
+}
